import { createFileRoute, Link } from "@tanstack/react-router";
import { BriefcaseBusiness, CalendarDays, Sparkles } from "lucide-react";
import { Logo } from "@/components/layout/Logo";
import { useAuth } from "@/hooks/use-auth";

export const Route = createFileRoute("/")({ head: () => ({ meta: [{ title: "KKU PART-TiME" }, { name: "description", content: "งาน Part-time ที่เหมาะกับนักศึกษา KKU" }] }), component: Landing });
function Landing() { const { user, role } = useAuth(); const home = user && role === "student" ? "/student/dashboard" : "/login"; return <div className="min-h-screen bg-background"><nav className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link to="/" className="flex items-center gap-2"><Logo/><b className="font-display">KKU PART-TiME</b></Link><div className="flex gap-3"><Link to="/login" className="px-3 py-2 text-sm">เข้าสู่ระบบ</Link><Link to="/register" className="rounded-md bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">สมัครใช้งาน</Link></div></nav><main className="mx-auto max-w-6xl px-5 py-24"><div className="max-w-3xl"><div className="inline-flex items-center gap-2 rounded-full bg-primary/10 px-3 py-1 text-xs font-bold text-primary"><Sparkles className="h-3.5 w-3.5"/>KKU STUDENT PLATFORM</div><h1 className="mt-6 font-display text-5xl font-bold tracking-tight md:text-7xl">งาน Part-time ที่เข้ากับตารางชีวิตของคุณ</h1><p className="mt-5 max-w-2xl text-lg text-muted-foreground">ค้นหางานที่ตรงกับทักษะ ตารางเรียน เวลาว่าง และเป้าหมายทางการเงินของนักศึกษา KKU</p><Link to={home as never} className="mt-8 inline-flex rounded-md bg-primary px-6 py-3 font-bold text-primary-foreground">เริ่มค้นหางาน</Link></div><section className="mt-20 grid gap-4 md:grid-cols-3">{[[BriefcaseBusiness,"Smart Job Matching","แนะนำงานจากตารางเรียน ทักษะ และความต้องการ"],[CalendarDays,"Schedule First","ตรวจเวลาชนก่อนตัดสินใจสมัครงาน"],[Sparkles,"Money Management","ติดตามรายได้ งาน และงบประมาณในที่เดียว"]].map(([Icon,title,text]: any) => <div key={title} className="rounded-xl border border-border bg-card p-6"><Icon className="h-6 w-6 text-primary"/><h2 className="mt-4 font-display text-xl font-bold">{title}</h2><p className="mt-2 text-sm text-muted-foreground">{text}</p></div>)}</section></main></div>; }
